import clsx from "clsx";

export const SkeletonCategoryRadarChartDiagnosis = () => {
  return (
    <div
      className={clsx(
        "rounded-[0.5rem]",
        "w-full h-[456px]",
        "animate-pulse",
        "bg-charleston-green"
      )}
    />
  );
};
